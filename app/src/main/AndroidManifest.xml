<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.madcourse.numad21sp_colinriley">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_cr"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_cr_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21SpColinRiley">
        <activity android:name=".LinkCollectorActivity"
            android:parentActivityName=".MainActivity">
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".ClickyActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.NUMAD21SpColinRiley.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>